  <!--Main Navigation-->
  <nav class="navbar navbar-expand-lg navbar-dark bg-dark">
    <div class="container-fluid">
      <a class="navbar-brand" href="/">COMP229</a>
      <!--hamburger like button-->
      <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
        <span class="navbar-toggler-icon"></span>
      </button>
      <!--location of navbar-->
      <div class="collapse navbar-collapse" id="navbarSupportedContent">
        <ul class="navbar-nav me-auto mb-2 mb-lg-0">
        <!--Navbar content-->
        <!--Webpages-->
          <li class="nav-item">
            <% if (title == "Home") { %>
              <a class="nav-link active" href="/home"><i class="fas fa-lg fa-laptop-house"></i> Home</a>
            <% } else { %>
              <a class="nav-link" href="/home"><i class="fas fa-lg fa-laptop-house"></i> Home</a>
            <% } %>
          </li>

          <li class="nav-item">
            <% if (title == "About") { %>
              <a class="nav-link active" href="/about"><i class="fas fa-info-circle"></i> About</a>
            <% } else { %>
              <a class="nav-link" href="/about"><i class="fas fa-info-circle"></i> About</a>
            <% } %>
          </li>

          <li class="nav-item">
            <% if (title == "Products") { %>
              <a class="nav-link active" href="/products"><i class="fas fa-tools"></i> Products</a>
            <% } else { %>
              <a class="nav-link" href="/products"><i class="fas fa-tools"></i> Products</a>
            <% } %>
          </li>

          <li class="nav-item">
            <% if (title == "Services") { %>
              <a class="nav-link active" href="/services"><i class="fas fa-concierge-bell"></i> Services</a>
            <% } else { %>
              <a class="nav-link" href="/services"><i class="fas fa-concierge-bell"></i> Services</a>
            <% } %>
          </li>

          <li class="nav-item">
            <% if (title == "Books") { %>
              <a class="nav-link active" href="/book-list"><i class="fas fa-book-open"></i> Books</a>
            <% } else { %>
              <a class="nav-link" href="/book-list"><i class="fas fa-book-open"></i> Books</a>
            <% } %>
          </li>

          <li class="nav-item">
            <% if (title == "Contact") { %>
              <a class="nav-link active" href="/contact"><i class="fas fa-phone"></i> Contact</a>
            <% } else { %>
              <a class="nav-link" href="/contact"><i class="fas fa-phone"></i> Contact</a>
            <% } %>
          </li>
          
          <% if(typeof displayName == 'string' && displayName) { %>
            <li class="navbar-text"><i class="fas fa-lg fa-user"></i> Welcome, <%= displayName %></li>
            <li class="nav-item"><a href="/logout" class="nav-link">
              <i class="fas fa-lg fa-sign-out-alt"></i> Logout</a></li>
          <% } else { %>
            <li class="nav-item"><a href="/login" class="nav-link">
              <i class="fas fa-lg fa-sign-in-alt"></i> Login</a></li>
          <% } %>
      </div>
    </div>
  </nav>